# Downpeed Agent Guide

本文件适用于 `downpeed-fluter/` 及其全部子目录。实现功能前先阅读根目录 `README.md`；它是产品范围、架构路线和里程碑状态的事实来源。

## Engineering Constraints

1. 严格按 `README.md` 的里程碑和清单推进。每完成并验证一个步骤，同步更新 README；不得提前勾选尚未形成可运行闭环的功能。
2. Go 内核是下载任务、协议状态和持久化状态的唯一事实源。Flutter 只保存主题、语言、布局等展示偏好，不复制任务状态机或持久化业务状态。
3. Flutter 页面模块统一采用 `binding/controller/view` 结构，并沿用 `AppInitializer → DependencyRegistrar → GlobalBinding → GetMaterialApp` 启动链。
4. Flutter UI 只依赖稳定领域模型与 `EngineClient` 契约；REST API 使用版本化 `/api/vN` 路径，FFI 只处理引擎生命周期。
5. 参考 Gopeed 的产品与架构思路，但不得复制其 GPLv3 源码、品牌素材、图标、包标识或其他受保护资产。引入第三方下载组件前必须完成许可证审查。
6. 本地 Go 服务默认只监听 Loopback；不得在日志和错误中泄露 URL 凭据、Cookie、自定义 Header、代理密码或本地敏感路径。
7. 修改范围保持聚焦，保留用户已有改动，不编辑生成目录与构建产物。

## Required Validation

完成相关改动后至少运行：

```text
backend: gofmt -w <changed-go-files>
backend: go test ./...
app:     dart format <changed-dart-files>
app:     dart analyze
app:     flutter test
```

涉及桌面原生集成或发布配置时，还需运行对应平台的 Flutter Debug 构建。无法运行的检查必须在交付说明中明确原因。

## Design Context

### Users

Downpeed 面向需要稳定管理本机下载任务的桌面用户，包括日常下载 HTTP 文件的普通用户，以及需要批量任务、限速、代理、BT/Magnet 和远程控制的下载重度用户。核心任务是在不被复杂配置干扰的前提下，快速添加任务、判断传输状态、处理失败并找到已下载文件。首期以 macOS、Windows 和 Linux 桌面端为主，移动端与 Web 端保留架构兼容性但不进入 MVP。

### Brand Personality

冷静、可靠、利落。界面应像一台安静工作的精密传输设备：状态明确、反馈及时、长期运行不令人疲惫。品牌不追求“极速”口号和夸张视觉，而通过稳定、可控和透明建立信任。

### Aesthetic Direction

参考 Codex 桌面端的沉浸式窗口关系、紧凑信息密度和中性工具气质，但不复制其品牌资产。macOS 内容延伸到透明标题栏下方，保留原生窗口按钮而不显示独立标题条；Windows/Linux 保持等价的无冗余顶栏结构。浅色主题使用近白工作区、雾灰侧栏、墨色正文和低对比细线；深色主题使用从靛紫过渡到海军蓝的整窗背景、同色系侧栏与带靛蓝色相的午夜色内容表面，选中态使用低饱和紫灰。主操作采用墨色/反色，不设置装饰性品牌彩色；成功、警告、错误保留独立语义色。

品牌标记采用原创“D 形下载通道”几何：外轮廓对应产品首字母，内部向下箭头表达文件进入本地设备。应用内标记跟随主题在墨色与暖白前景间切换，平台启动图标使用中性 Graphite 底板保持稳定识别。

字体优先使用平台系统字体，中文回退 PingFang SC / Microsoft YaHei / Noto Sans CJK；页面标题 19px、正文 13px、控件标签 12.5px，标题最多使用 600 字重，正文保持 400，控件与强调信息使用 500，数据与数量使用等宽数字。桌面控件默认高度 32px，常规图标 15px，并使用 Lucide 300 线宽版本；分段选择、开关、按钮和菜单必须使用紧凑桌面形态，不能回退为 Material 默认的大胶囊、大触控区或厚线图标。产品的视觉记忆点仍是“传输轨道”：任务进度不只是一根普通进度条，而是能表达分段、活动、等待和完成状态的细轨道。桌面宽屏采用侧栏、任务工作区和可选详情检查器；窄屏切换为单栏导航与独立详情页。引擎连接状态应作为任务工作区标题旁的内联辅助信息，不能在侧栏底部伪装成导航项；侧栏底部只承载设置与主题操作。新建下载必须作为任务工作区上方的响应式模态流程，桌面居中、窄屏近全屏，不得为常规入口切换独立页面；独立路由只保留兼容用途。设置页在宽屏使用左侧分组导航与右侧单分组内容，窄屏改为菜单到详情的主从导航，不能把所有设置卡片重新堆回一个长页面。避免营销式仪表盘、满屏指标卡、玻璃拟态、霓虹渐变、夸张阴影和嵌套卡片。

### Design Principles

1. 任务优先：启动后直接进入可操作的任务工作区，添加任务始终在一跳之内。
2. 状态可读：速度、进度、剩余时间和错误原因必须一眼可辨，不只依赖颜色。
3. 安静运行：进行中的传输有生命感但不过度动画，尊重减少动态效果设置。
4. 渐进披露：常用下载只需要 URL 与保存位置，连接数、Header、代理等高级项按需展开。
5. 原生对齐：尊重 macOS 窗口控件、Windows/Linux 桌面习惯、键盘操作和系统托盘。
6. 单一事实源：Go 内核拥有任务和持久化状态，Flutter 只负责呈现与交互，避免双向状态漂移。
7. 可访问与稳健：支持深浅主题、200% 文字缩放、键盘焦点、读屏语义和长文件名边界。
8. 破坏性操作分层：删除任务必须确认，删除记录与删除本地文件必须是两个明确选项且默认保留文件；“清空已完成”只移除记录，不能隐式删除下载文件。
9. BT 元数据渐进披露：Torrent 文件使用有高度上限的紧凑虚拟列表，不渲染无限嵌套目录树；长路径必须省略但保留可选择语义和独立体积信息。
10. 网络边界可见：Magnet 只有身份、尚未取得元数据时必须明确说明；任何 Tracker、DHT、Peer、WebSeed 或上传活动都必须在独立阶段经过安全配置和用户可见提示后才能启用。
