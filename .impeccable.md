## Design Context

### Users

Downpeed 面向需要稳定管理本机下载任务的桌面用户，包括日常下载 HTTP 文件的普通用户，以及需要批量任务、限速、代理、BT/Magnet 和远程控制的下载重度用户。核心任务是在不被复杂配置干扰的前提下，快速添加任务、判断传输状态、处理失败并找到已下载文件。首期以 macOS、Windows 和 Linux 桌面端为主，移动端与 Web 端保留架构兼容性但不进入 MVP。

### Brand Personality

冷静、可靠、利落。界面应像一台安静工作的精密传输设备：状态明确、反馈及时、长期运行不令人疲惫。品牌不追求“极速”口号和夸张视觉，而通过稳定、可控和透明建立信任。

### Aesthetic Direction

参考 Codex 桌面端的沉浸式窗口关系、紧凑信息密度和中性工具气质，但不复制其品牌资产。macOS 内容延伸到透明标题栏下方，保留原生窗口按钮而不显示独立标题条；Windows/Linux 保持等价的无冗余顶栏结构。浅色主题使用近白工作区、雾灰侧栏、墨色正文和低对比细线，深色主题使用炭黑工作区与逐级提亮的内容表面。主操作采用墨色/反色，不设置装饰性品牌彩色；成功、警告、错误保留独立语义色。

字体优先使用平台系统字体，中文回退 PingFang SC / Microsoft YaHei / Noto Sans CJK；标题最多使用 600 字重，正文保持 400，数据与数量使用等宽数字。产品的视觉记忆点仍是“传输轨道”：任务进度不只是一根普通进度条，而是能表达分段、活动、等待和完成状态的细轨道。桌面宽屏采用侧栏、任务工作区和可选详情检查器；窄屏切换为单栏导航与独立详情页。避免营销式仪表盘、满屏指标卡、玻璃拟态、霓虹渐变、夸张阴影和嵌套卡片。

### Design Principles

1. 任务优先：启动后直接进入可操作的任务工作区，添加任务始终在一跳之内。
2. 状态可读：速度、进度、剩余时间和错误原因必须一眼可辨，不只依赖颜色。
3. 安静运行：进行中的传输有生命感但不过度动画，尊重减少动态效果设置。
4. 渐进披露：常用下载只需要 URL 与保存位置，连接数、Header、代理等高级项按需展开。
5. 原生对齐：尊重 macOS 窗口控件、Windows/Linux 桌面习惯、键盘操作和系统托盘。
6. 单一事实源：Go 内核拥有任务和持久化状态，Flutter 只负责呈现与交互，避免双向状态漂移。
7. 可访问与稳健：支持深浅主题、200% 文字缩放、键盘焦点、读屏语义和长文件名边界。
